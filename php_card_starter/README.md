# How to run PHP starter
* cd into dir
* terminal cmd to start server: php -S localhost:8000
* open browser -> http://localhost:8000

# Install web servers locally
* Wamp -> Windows
* XAMPP -> other OS
